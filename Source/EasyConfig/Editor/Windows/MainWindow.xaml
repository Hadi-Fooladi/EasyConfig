<Window
	x:ClassModifier="internal"
	x:Class="Editor.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:local="clr-namespace:Editor"
	mc:Ignorable="d"
	Title="EasyConfig Editor"
	Width="640" Height="480"
	FontSize="16"
	WindowStartupLocation="CenterScreen">
	<DockPanel>

		<!-- Main Menu -->
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_File">
				<MenuItem Header="_Open" Click="miOpen_OnClick"  />
				<Separator />
				<MenuItem Header="_Save" Click="miSave_OnClick"  />
				<Separator />
				<MenuItem Header="E_xit" Click="miExit_OnClick" />
			</MenuItem>
			<MenuItem Header="_Schema">
				<MenuItem Header="_Open" Click="miOpenSchema_OnClick"  />
			</MenuItem>
		</Menu>

		<TreeView
			Name="TV" DockPanel.Dock="Left" Width="175"
			PreviewKeyUp="TV_OnPreviewKeyUp"
			PreviewKeyDown="TV_OnPreviewKeyDown"
			SelectedItemChanged="TV_OnSelectedItemChanged">
			
			<TreeView.Resources>
				<SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="{x:Static SystemColors.HighlightColor}" />
				<SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{x:Static SystemColors.HighlightTextColor}" />
			</TreeView.Resources>
		</TreeView>

		<TextBlock Name="lblPath" DockPanel.Dock="Top" Text="{Binding Path}" FontSize="20" FontWeight="Bold" Margin="5" />

		<!-- Attributes Grid -->
		<DataGrid Name="DG" AutoGenerateColumns="False" CanUserDeleteRows="False" SelectionMode="Single" SelectionUnit="FullRow">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" />
				<DataGridTextColumn Header="Type" Binding="{Binding Type}" IsReadOnly="True" />
				<DataGridTemplateColumn Header="Override Default">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<CheckBox IsEnabled="{Binding HasDefault}" IsChecked="{Binding OverrideDefault,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn Header="Value" Binding="{Binding Value,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Width="*" />
				<DataGridTextColumn Header="Default" Binding="{Binding Default}" IsReadOnly="True" Width="80" />
			</DataGrid.Columns>
		</DataGrid>
	</DockPanel>
</Window>
